ame: Build Windows Executable

on:
  push:
#    branches: [ main ]
    tags:
      - '*'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  build:
    runs-on: windows-latest
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v4
    
    - name: Set up Python
      uses: actions/setup-python@v5
      with:
        python-version: '3.11'
        architecture: 'x64'
    
    - name: Install dependencies and download corpora
      run: |
        python -m pip install --upgrade pip
        pip install pyodbc sqlparse textblob
    
    - name: Build Windows Executable
      shell: pwsh
      run: |
        python -m nuitka --standalone --onefile --msvc=latest --enable-plugin=anti-bloat --output-dir=build --output-filename=scaffold_msaccess.exe scaffold_msaccess.py
    
    - name: Upload Artifact
      uses: actions/upload-artifact@v4
      with:
        name: scaffold_msaccess_windows_x86_64
        path: build/scaffold_msaccess.exe
        if-no-files-found: error
